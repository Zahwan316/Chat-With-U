type group = {
    id: string,
    Admin_user_id: string,
    name: string,
    description: string,
    visibility: "public" | "private",
    created_date: string,
    img: string
}

export default group